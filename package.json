{
  "name": "beyond-media-suite",
  "private": true,
  "version": "2.1.3-26",
  "description": "Beyond Media Suite: a configurable local image, animation, video, collage, and YouTube clip creator.",
  "type": "module",
  "scripts": {
    "build:fork": "node scripts/build-fork.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "@electron/asar": "4.0.1",
    "prettier": "3.6.2"
  }
}
