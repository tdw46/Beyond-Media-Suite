{
  "name": "xpic-target-size-fork",
  "private": true,
  "version": "2.1.3-20",
  "description": "Reproducible xPic 2.1.3 fork build with per-format target output sizes.",
  "type": "module",
  "scripts": {
    "build:fork": "node scripts/build-fork.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "@electron/asar": "4.0.1",
    "prettier": "3.6.2"
  }
}
